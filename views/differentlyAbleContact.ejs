<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Madhav Sewa Society</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" type="text/css"
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css">
        <link rel="icon" href="images/mss-logo.png">

</head>
<style>
    /* Responsive grid layout for contact cards */
    :root {
      --light-orange-color: #fff8f1;
      --dark-orange-color: #ffa85a;
      --fade-light-orange-color: #fff8f180;

    }

    ::-webkit-scrollbar {
      width: 5px;
      background-color: var(--dark-orange-color);
    }

    ::-webkit-scrollbar-thumb {
      background: var(--light-orange-color);
    }

    * {
      margin: 0;
      box-sizing: border-box;
      padding: 0;
      font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
    }

    body {
      margin: 0;
      padding: 0;
      background-color: var(--light-orange-color);
    }

    .container{
        margin-top: 3rem;
    }
    .contact-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      grid-gap: 20px;
      margin: 20px auto;
      width: 80%;
    }

    .contact-card {
      background-color: white;
      border-radius: 12px;
      padding: 15px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }

    .contact-image {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      margin-bottom: 10px;
    }
    .contact-card a{
        text-decoration: none;
    }
    .contact-name {
      font-weight: bold;
      font-size: 18px;
      margin-bottom: 5px;
    }

    .contact-info {
      font-size: 14px;
      margin-bottom: 5px;
    }

    .contact-dob {
      font-weight: bold;
      color: #333;
    }

    .certificate-img{
        background-color: var(--dark-orange-color);
        height: 40px;
        color: white;
        border-radius: 8px;
        margin: 1rem;
        display: flex;
        justify-content: center;
        align-items: center;
        border: 1px solid transparent;
        transition: all 0.2s ease-in-out;
        cursor: pointer;
    }
    .certificate-img:hover{
        background-color: var(--light-orange-color);
        color: black;
        border: 1px solid var(--dark-orange-color);
    }
    .nameAndDate{
        display: flex;
        justify-content: space-between;
    }

    .nameAndDate p{
        font-size: 13px;
    }

    .accept-reject{
        display: flex;
        justify-content: end;
        gap: 1rem;
        margin: 1rem;
    }
  </style>
<body>
  <nav class="navbar navbar-expand-lg navbar-light p-3" style="background-color: #ffa85a ;">
    <div class="container-fluid">
      <a class="navbar-brand" href="#" style="color: white;">Madhav Sewa Society </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
        aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class=" collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav ms-auto ">
          <li class="nav-item">
            <a class="nav-link mx-2" aria-current="page" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link mx-2" href="/volunteer">Volunteer</a>
          </li>
          <li class="nav-item">
            <a class="nav-link mx-2" href="/dailyMotivation">Daily Motivation & Mudras</a>
          </li>

          <li class="nav-item">
            <a class="nav-link mx-2" href="/donations">Donations</a>
          </li>
           <li class="nav-item">
            <a class="nav-link mx-2 active" href="/differentlyAbleContactForm">Differently able Contact</a>
          </li> 
          <li class="nav-item">
            <a class="nav-link mx-2" href="/connectWithUs">Connect With Us</a>
          </li>
        </ul>
        <ul class="navbar-nav ms-auto d-none d-lg-inline-flex">
          <li class="nav-item mx-2">
            
          </li>
          <li class="nav-item mx-2">
            
          </li>
          <li class="nav-item mx-2">
            
          </li>
        </ul>
      </div>
    </div>
  </nav>

    <main>
        <div class="container">

            <h1>Differently Abled Forms</h1>
            <div class="accept-reject">
                <a href="/differentlyAbleContactForm/accepted" style="color: skyblue;">Accepted forms</a>
                <a href="/differentlyAbleContactForm/rejected" style="color: red;" >Rejected forms</a>
            </div>
        </div>
  <div class="contact-grid">
    <%  var statuss = status; %>
    <% for (const contact of contacts) { %>
        <% if(statuss === contact.status){ %>
      <div class="contact-card">
        <a target="_blank" href="<%=contact.img_url  %>">
        <div class="certificate-img">
            <h5>Disability Certificate</h5>
        </div>
    </a>
        <div class="nameAndDate">
            <h3 class="contact-name"><%= contact.name %></h3> 
            <p class="contact-dateAndTime"><%= contact.dateAndTime %></> 
        </div>
        <p class="contact-info"><b>Email:</b> <%= contact.email %></p>
        <p class="contact-info"><b>Phone:</b> <%= contact.phone %></p>
        <p class="contact-info"><b>Gender:</b> <%= contact.gender %></p>
        <p class="contact-info"><b>Father Name:</b> <%= contact.father %></p>
        <p class="contact-info"><b>Mother Name:</b> <%= contact.mother %></p>
        <p class="contact-info"><b>Qualifications:</b> <%= contact.qualifications %></p>
        <p class="contact-info"><b>Percentage of disability:</b> <%= contact.percentage %></p>
        <p class="contact-info"><b>Services Needed:</b> <%= contact.services %></p>
        <%  if(status === '0'){%> 
        <div>
          <a href="/differentlyAbleContactForm/accept/<%=contact._id  %>"><button class="btn btn-primary">Accept</button></a>
          <a href="/differentlyAbleContactForm/reject/<%=contact._id  %>"><button class="btn btn-danger">Reject</button></a>
        </div>
        <% } %>
      </div>

    <% } %>
    <% } %>
  </div>
    </main>
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</html>
